"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET_SENTINEL,
)
from clerk_backend_api.utils import FieldMetadata, PathParamMetadata, RequestMetadata
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class IdentificationStatus(str, Enum):
    r"""Controls the status of the replacement email address. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked so no
    other user can claim it), or to `unverified` to create it neither usable for sign-in
    nor locked.

    **Warning:** `unverified` can lock the user out of their account. An unverified email
    address cannot be used to sign in, so if the user has no other verified or reserved
    identifier, they will be unable to authenticate and unable to verify this address.
    Prefer `reserved` unless you specifically need the address left unclaimed — for
    example so that another user can also hold it until one of them verifies it.
    """

    VERIFIED = "verified"
    RESERVED = "reserved"
    UNVERIFIED = "unverified"


class ReplaceUserEmailAddressRequestBodyTypedDict(TypedDict):
    email_address: str
    r"""The new email address. Must adhere to the RFC 5322 specification for email address format."""
    identification_status: NotRequired[IdentificationStatus]
    r"""Controls the status of the replacement email address. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked so no
    other user can claim it), or to `unverified` to create it neither usable for sign-in
    nor locked.

    **Warning:** `unverified` can lock the user out of their account. An unverified email
    address cannot be used to sign in, so if the user has no other verified or reserved
    identifier, they will be unable to authenticate and unable to verify this address.
    Prefer `reserved` unless you specifically need the address left unclaimed — for
    example so that another user can also hold it until one of them verifies it.
    """
    notify_primary_email_address_changed: NotRequired[Nullable[bool]]
    r"""If set to `true`, the user's previous primary email address is notified that the
    primary email address has changed. No notification is sent when the replacement
    is the user's current primary email address. By default, no notification is sent.
    """


class ReplaceUserEmailAddressRequestBody(BaseModel):
    email_address: str
    r"""The new email address. Must adhere to the RFC 5322 specification for email address format."""

    identification_status: Optional[IdentificationStatus] = (
        IdentificationStatus.VERIFIED
    )
    r"""Controls the status of the replacement email address. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked so no
    other user can claim it), or to `unverified` to create it neither usable for sign-in
    nor locked.

    **Warning:** `unverified` can lock the user out of their account. An unverified email
    address cannot be used to sign in, so if the user has no other verified or reserved
    identifier, they will be unable to authenticate and unable to verify this address.
    Prefer `reserved` unless you specifically need the address left unclaimed — for
    example so that another user can also hold it until one of them verifies it.
    """

    notify_primary_email_address_changed: OptionalNullable[bool] = False
    r"""If set to `true`, the user's previous primary email address is notified that the
    primary email address has changed. No notification is sent when the replacement
    is the user's current primary email address. By default, no notification is sent.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["identification_status", "notify_primary_email_address_changed"]
        )
        nullable_fields = set(["notify_primary_email_address_changed"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class ReplaceUserEmailAddressRequestTypedDict(TypedDict):
    user_id: str
    r"""The ID of the user whose email address to replace"""
    request_body: ReplaceUserEmailAddressRequestBodyTypedDict


class ReplaceUserEmailAddressRequest(BaseModel):
    user_id: Annotated[
        str, FieldMetadata(path=PathParamMetadata(style="simple", explode=False))
    ]
    r"""The ID of the user whose email address to replace"""

    request_body: Annotated[
        ReplaceUserEmailAddressRequestBody,
        FieldMetadata(request=RequestMetadata(media_type="application/json")),
    ]
