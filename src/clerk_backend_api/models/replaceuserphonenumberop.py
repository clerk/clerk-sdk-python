"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from clerk_backend_api.utils import FieldMetadata, PathParamMetadata, RequestMetadata
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ReplaceUserPhoneNumberIdentificationStatus(str, Enum):
    r"""Controls the status of the replacement phone number. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked)
    instead of verified.
    """

    VERIFIED = "verified"
    RESERVED = "reserved"


class ReplaceUserPhoneNumberRequestBodyTypedDict(TypedDict):
    phone_number: str
    r"""The new phone number. Must adhere to the E.164 standard for phone number format."""
    identification_status: NotRequired[ReplaceUserPhoneNumberIdentificationStatus]
    r"""Controls the status of the replacement phone number. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked)
    instead of verified.
    """


class ReplaceUserPhoneNumberRequestBody(BaseModel):
    phone_number: str
    r"""The new phone number. Must adhere to the E.164 standard for phone number format."""

    identification_status: Optional[ReplaceUserPhoneNumberIdentificationStatus] = (
        ReplaceUserPhoneNumberIdentificationStatus.VERIFIED
    )
    r"""Controls the status of the replacement phone number. Defaults to `verified`. Set to
    `reserved` to create it reserved (unverified but usable for sign-in and locked)
    instead of verified.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["identification_status"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ReplaceUserPhoneNumberRequestTypedDict(TypedDict):
    user_id: str
    r"""The ID of the user whose phone number to replace"""
    request_body: ReplaceUserPhoneNumberRequestBodyTypedDict


class ReplaceUserPhoneNumberRequest(BaseModel):
    user_id: Annotated[
        str, FieldMetadata(path=PathParamMetadata(style="simple", explode=False))
    ]
    r"""The ID of the user whose phone number to replace"""

    request_body: Annotated[
        ReplaceUserPhoneNumberRequestBody,
        FieldMetadata(request=RequestMetadata(media_type="application/json")),
    ]
