"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class VerificationResponseTypedDict(TypedDict):
    r"""The verification. After prepare_verification it is pending (unverified);
    after attempt_verification its status reflects the attempt outcome.
    """

    object: NotRequired[str]
    r"""The type of the verification object."""
    id: NotRequired[str]
    r"""The ID of the verification. Pass this to attempt_verification."""
    status: NotRequired[str]
    r"""The status of the verification (unverified, verified, expired, or failed)."""
    strategy: NotRequired[str]
    r"""The verification strategy (email_code or phone_code)."""
    attempts: NotRequired[Nullable[int]]
    r"""The number of attempts made against this verification."""
    expire_at: NotRequired[Nullable[int]]
    r"""Unix timestamp (milliseconds) at which the code expires."""
    channel: NotRequired[Nullable[str]]
    r"""The channel the code was sent over (phone numbers only)."""


class VerificationResponse(BaseModel):
    r"""The verification. After prepare_verification it is pending (unverified);
    after attempt_verification its status reflects the attempt outcome.
    """

    object: Optional[str] = None
    r"""The type of the verification object."""

    id: Optional[str] = None
    r"""The ID of the verification. Pass this to attempt_verification."""

    status: Optional[str] = None
    r"""The status of the verification (unverified, verified, expired, or failed)."""

    strategy: Optional[str] = None
    r"""The verification strategy (email_code or phone_code)."""

    attempts: OptionalNullable[int] = UNSET
    r"""The number of attempts made against this verification."""

    expire_at: OptionalNullable[int] = UNSET
    r"""Unix timestamp (milliseconds) at which the code expires."""

    channel: OptionalNullable[str] = UNSET
    r"""The channel the code was sent over (phone numbers only)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["object", "id", "status", "strategy", "attempts", "expire_at", "channel"]
        )
        nullable_fields = set(["attempts", "expire_at", "channel"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
