"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, Nullable, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class GroupsTypedDict(TypedDict):
    id: str
    display_name: str


class Groups(BaseModel):
    id: str

    display_name: str


class SCIMUserMetadataTypedDict(TypedDict):
    r"""Metadata describing a user's linkage to a directory. Included in user responses when directory data is requested, and in directory-triggered user webhooks. Its absence does not necessarily mean the user is not managed by a directory."""

    id: str
    r"""The user's resource ID in this directory."""
    directory_name: str
    provider: str
    enterprise_connection_id: Nullable[str]
    directory_id: str
    r"""The ID of the directory the user is provisioned from.

    """
    directory_enabled: bool
    r"""Whether the directory is currently enabled.

    """
    external_id: Nullable[str]
    r"""The user's external ID as reported by the directory, if any.

    """
    groups: NotRequired[List[GroupsTypedDict]]
    r"""Omitted when groups were not loaded; an empty array means no group memberships."""


class SCIMUserMetadata(BaseModel):
    r"""Metadata describing a user's linkage to a directory. Included in user responses when directory data is requested, and in directory-triggered user webhooks. Its absence does not necessarily mean the user is not managed by a directory."""

    id: str
    r"""The user's resource ID in this directory."""

    directory_name: str

    provider: str

    enterprise_connection_id: Nullable[str]

    directory_id: str
    r"""The ID of the directory the user is provisioned from.

    """

    directory_enabled: bool
    r"""Whether the directory is currently enabled.

    """

    external_id: Nullable[str]
    r"""The user's external ID as reported by the directory, if any.

    """

    groups: Optional[List[Groups]] = None
    r"""Omitted when groups were not loaded; an empty array means no group memberships."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["groups"])
        nullable_fields = set(["enterprise_connection_id", "external_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
