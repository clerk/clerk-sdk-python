"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .role import Role, RoleTypedDict
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class DirectoryGroupRoleMappingObject(str, Enum):
    r"""String representing the object's type. Always \"directory_group_role_mapping\"."""

    DIRECTORY_GROUP_ROLE_MAPPING = "directory_group_role_mapping"


class DirectoryGroupRoleMappingTypedDict(TypedDict):
    r"""A mapping from a directory group to an organization role. Members of the group are assigned the role; the lowest-precedence match wins when a user belongs to multiple mapped groups."""

    object: DirectoryGroupRoleMappingObject
    r"""String representing the object's type. Always \"directory_group_role_mapping\"."""
    id: str
    r"""Unique identifier for the directory group role mapping."""
    directory_id: str
    r"""The ID of the directory this mapping belongs to."""
    scim_directory_id: str
    r"""The legacy name for `directory_id`. Carries the same value."""
    directory_group_id: str
    r"""The group ID from the identity provider."""
    scim_group_id: str
    r"""The legacy name for `directory_group_id`. Carries the same value."""
    directory_group_display_name: str
    r"""The display name of the group, as reported by the identity provider."""
    scim_group_display_name: str
    r"""The legacy name for `directory_group_display_name`. Carries the same value."""
    precedence: int
    r"""Mapping precedence, starting at 1. Lower values take priority when a user belongs to multiple mapped groups."""
    created_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was created."""
    updated_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was last updated."""
    role: NotRequired[RoleTypedDict]


class DirectoryGroupRoleMapping(BaseModel):
    r"""A mapping from a directory group to an organization role. Members of the group are assigned the role; the lowest-precedence match wins when a user belongs to multiple mapped groups."""

    object: DirectoryGroupRoleMappingObject
    r"""String representing the object's type. Always \"directory_group_role_mapping\"."""

    id: str
    r"""Unique identifier for the directory group role mapping."""

    directory_id: str
    r"""The ID of the directory this mapping belongs to."""

    scim_directory_id: str
    r"""The legacy name for `directory_id`. Carries the same value."""

    directory_group_id: str
    r"""The group ID from the identity provider."""

    scim_group_id: str
    r"""The legacy name for `directory_group_id`. Carries the same value."""

    directory_group_display_name: str
    r"""The display name of the group, as reported by the identity provider."""

    scim_group_display_name: str
    r"""The legacy name for `directory_group_display_name`. Carries the same value."""

    precedence: int
    r"""Mapping precedence, starting at 1. Lower values take priority when a user belongs to multiple mapped groups."""

    created_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was created."""

    updated_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was last updated."""

    role: Optional[Role] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["role"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
