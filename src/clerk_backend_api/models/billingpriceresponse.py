"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from enum import Enum
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class BillingPriceResponseObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    COMMERCE_PRICE = "commerce_price"


class BillingPriceResponseFeeTypedDict(TypedDict):
    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class BillingPriceResponseFee(BaseModel):
    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class BillingPriceResponseAnnualMonthlyFeeTypedDict(TypedDict):
    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class BillingPriceResponseAnnualMonthlyFee(BaseModel):
    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class BillingPriceResponseTypedDict(TypedDict):
    object: BillingPriceResponseObject
    r"""String representing the object's type. Objects of the same type share the same value."""
    id: str
    r"""Unique identifier for the price."""
    plan_id: str
    r"""Unique identifier for the associated plan."""
    instance_id: str
    r"""Unique identifier for the instance."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""
    amount: int
    r"""The amount in cents for the price."""
    annual_monthly_amount: int
    r"""The monthly amount in cents when billed annually."""
    fee: Nullable[BillingPriceResponseFeeTypedDict]
    annual_monthly_fee: Nullable[BillingPriceResponseAnnualMonthlyFeeTypedDict]
    is_default: bool
    r"""Whether this price is the default price for its plan."""
    created_at: int
    r"""Unix timestamp (milliseconds) of creation."""
    description: NotRequired[Nullable[str]]
    r"""The description of the price."""


class BillingPriceResponse(BaseModel):
    object: BillingPriceResponseObject
    r"""String representing the object's type. Objects of the same type share the same value."""

    id: str
    r"""Unique identifier for the price."""

    plan_id: str
    r"""Unique identifier for the associated plan."""

    instance_id: str
    r"""Unique identifier for the instance."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""

    amount: int
    r"""The amount in cents for the price."""

    annual_monthly_amount: int
    r"""The monthly amount in cents when billed annually."""

    fee: Nullable[BillingPriceResponseFee]

    annual_monthly_fee: Nullable[BillingPriceResponseAnnualMonthlyFee]

    is_default: bool
    r"""Whether this price is the default price for its plan."""

    created_at: int
    r"""Unix timestamp (milliseconds) of creation."""

    description: OptionalNullable[str] = UNSET
    r"""The description of the price."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description"])
        nullable_fields = set(["fee", "annual_monthly_fee", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
