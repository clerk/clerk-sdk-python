"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class CreateDirectoryGroupRoleMappingsTypedDict(TypedDict):
    role_id: str
    r"""The organization role ID to assign to members of the group."""
    directory_group_id: NotRequired[str]
    r"""The group ID from the identity provider. Exactly one of `directory_group_id` or `scim_group_id` is required."""
    scim_group_id: NotRequired[str]
    r"""The legacy name for `directory_group_id`. Send either one, or both with the same value; sending both with different values is rejected."""
    directory_group_display_name: NotRequired[str]
    r"""Optional display name for the group."""
    scim_group_display_name: NotRequired[str]
    r"""The legacy name for `directory_group_display_name`. Send either one, or both with the same value; sending both with different values is rejected."""
    precedence: NotRequired[int]
    r"""The precedence for this mapping. If omitted, precedence is assigned by array order."""


class CreateDirectoryGroupRoleMappings(BaseModel):
    role_id: str
    r"""The organization role ID to assign to members of the group."""

    directory_group_id: Optional[str] = None
    r"""The group ID from the identity provider. Exactly one of `directory_group_id` or `scim_group_id` is required."""

    scim_group_id: Optional[str] = None
    r"""The legacy name for `directory_group_id`. Send either one, or both with the same value; sending both with different values is rejected."""

    directory_group_display_name: Optional[str] = None
    r"""Optional display name for the group."""

    scim_group_display_name: Optional[str] = None
    r"""The legacy name for `directory_group_display_name`. Send either one, or both with the same value; sending both with different values is rejected."""

    precedence: Optional[int] = None
    r"""The precedence for this mapping. If omitted, precedence is assigned by array order."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "directory_group_id",
                "scim_group_id",
                "directory_group_display_name",
                "scim_group_display_name",
                "precedence",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateDirectoryRequestBodyTypedDict(TypedDict):
    enterprise_connection_id: str
    r"""The ID of the enterprise connection to associate with this directory."""
    provider: str
    r"""The identity provider for this directory."""
    name: NotRequired[str]
    r"""A human-friendly name for the directory."""
    group_role_mappings: NotRequired[List[CreateDirectoryGroupRoleMappingsTypedDict]]
    r"""Optional directory group to organization role mappings to create with the directory."""


class CreateDirectoryRequestBody(BaseModel):
    enterprise_connection_id: str
    r"""The ID of the enterprise connection to associate with this directory."""

    provider: str
    r"""The identity provider for this directory."""

    name: Optional[str] = None
    r"""A human-friendly name for the directory."""

    group_role_mappings: Optional[List[CreateDirectoryGroupRoleMappings]] = None
    r"""Optional directory group to organization role mappings to create with the directory."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "group_role_mappings"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
