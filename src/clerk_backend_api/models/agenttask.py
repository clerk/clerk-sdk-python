"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class AgentTaskObject(str, Enum):
    AGENT_TASK = "agent_task"


class AgentTaskTypedDict(TypedDict):
    r"""Success"""

    object: AgentTaskObject
    agent_id: str
    r"""A stable identifier for the agent, unique per agent_name within an instance.

    """
    task_id: str
    r"""A unique identifier for this agent task.

    """
    url: NotRequired[str]
    r"""The URL that, when visited, creates a session for the user. Only present in the response to a create request.

    """


class AgentTask(BaseModel):
    r"""Success"""

    object: AgentTaskObject

    agent_id: str
    r"""A stable identifier for the agent, unique per agent_name within an instance.

    """

    task_id: str
    r"""A unique identifier for this agent task.

    """

    url: Optional[str] = None
    r"""The URL that, when visited, creates a session for the user. Only present in the response to a create request.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["url"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
