"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class CreateBillingPriceRequestTypedDict(TypedDict):
    plan_id: str
    r"""The ID of the plan this price belongs to."""
    amount: int
    r"""The amount in cents for the price. Must be at least $1 (100 cents)."""
    currency: NotRequired[str]
    r"""The currency code (e.g., \"USD\"). Defaults to USD."""
    annual_monthly_amount: NotRequired[int]
    r"""The monthly amount in cents when billed annually. Optional."""
    description: NotRequired[str]
    r"""An optional description for this custom price."""


class CreateBillingPriceRequest(BaseModel):
    plan_id: str
    r"""The ID of the plan this price belongs to."""

    amount: int
    r"""The amount in cents for the price. Must be at least $1 (100 cents)."""

    currency: Optional[str] = "USD"
    r"""The currency code (e.g., \"USD\"). Defaults to USD."""

    annual_monthly_amount: Optional[int] = None
    r"""The monthly amount in cents when billed annually. Optional."""

    description: Optional[str] = None
    r"""An optional description for this custom price."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["currency", "annual_monthly_amount", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
