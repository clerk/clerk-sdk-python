"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class OrganizationSettingsObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    ORGANIZATION_SETTINGS = "organization_settings"


class DomainsEnrollmentModes(str, Enum):
    MANUAL_INVITATION = "manual_invitation"
    AUTOMATIC_INVITATION = "automatic_invitation"
    AUTOMATIC_SUGGESTION = "automatic_suggestion"


class OrganizationSettingsTypedDict(TypedDict):
    r"""Success"""

    object: OrganizationSettingsObject
    r"""String representing the object's type. Objects of the same type share the same value."""
    enabled: bool
    max_allowed_memberships: int
    max_allowed_roles: int
    creator_role: str
    r"""The role key that a user will be assigned after creating an organization."""
    admin_delete_enabled: bool
    r"""The default for whether an admin can delete an organization with the Frontend API."""
    domains_enabled: bool
    domains_enrollment_modes: List[DomainsEnrollmentModes]
    domains_default_role: str
    r"""The role key that it will be used in order to create an organization invitation or suggestion."""
    max_role_sets_allowed: NotRequired[int]
    max_allowed_permissions: NotRequired[int]
    r"""max_allowed_permissions is now a no-op, as permissions are now unlimited"""
    slug_disabled: NotRequired[bool]
    initial_role_set_key: NotRequired[Nullable[str]]
    r"""The role set key that it will be used to create new organizations."""


class OrganizationSettings(BaseModel):
    r"""Success"""

    object: OrganizationSettingsObject
    r"""String representing the object's type. Objects of the same type share the same value."""

    enabled: bool

    max_allowed_memberships: int

    max_allowed_roles: int

    creator_role: str
    r"""The role key that a user will be assigned after creating an organization."""

    admin_delete_enabled: bool
    r"""The default for whether an admin can delete an organization with the Frontend API."""

    domains_enabled: bool

    domains_enrollment_modes: List[DomainsEnrollmentModes]

    domains_default_role: str
    r"""The role key that it will be used in order to create an organization invitation or suggestion."""

    max_role_sets_allowed: Optional[int] = None

    max_allowed_permissions: Annotated[
        Optional[int],
        pydantic.Field(
            deprecated="warning: ** DEPRECATED ** - This will be removed in a future release, please migrate away from it as soon as possible."
        ),
    ] = None
    r"""max_allowed_permissions is now a no-op, as permissions are now unlimited"""

    slug_disabled: Optional[bool] = None

    initial_role_set_key: OptionalNullable[str] = UNSET
    r"""The role set key that it will be used to create new organizations."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = [
            "max_role_sets_allowed",
            "max_allowed_permissions",
            "slug_disabled",
            "initial_role_set_key",
        ]
        nullable_fields = ["initial_role_set_key"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
