"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class OnBehalfOfTypedDict(TypedDict):
    r"""Identifies the user on whose behalf the agent task is created.
    Exactly one of user_id or identifier must be provided.
    """

    user_id: NotRequired[str]
    r"""The ID of the user."""
    identifier: NotRequired[str]
    r"""A verified identifier (e.g. email address) belonging to the user."""


class OnBehalfOf(BaseModel):
    r"""Identifies the user on whose behalf the agent task is created.
    Exactly one of user_id or identifier must be provided.
    """

    user_id: Optional[str] = None
    r"""The ID of the user."""

    identifier: Optional[str] = None
    r"""A verified identifier (e.g. email address) belonging to the user."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["user_id", "identifier"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateAgentTaskPermissions(str, Enum):
    r"""The permissions granted to the agent task. Must be \"*\" (all permissions)."""

    WILDCARD_ = "*"


class CreateAgentTaskRequestBodyTypedDict(TypedDict):
    on_behalf_of: OnBehalfOfTypedDict
    r"""Identifies the user on whose behalf the agent task is created.
    Exactly one of user_id or identifier must be provided.
    """
    permissions: CreateAgentTaskPermissions
    r"""The permissions granted to the agent task. Must be \"*\" (all permissions)."""
    agent_name: str
    r"""A name identifying the agent. Used to derive a stable agent_id per instance.
    Logged for audit purposes.
    """
    task_description: str
    r"""A description of the task being performed. Logged for audit purposes."""
    redirect_url: str
    r"""The URL the user is redirected to after the agent task is accepted.
    Must be a valid absolute URL with an `https` scheme in production instances.
    In development instances, `http` is also permitted.
    The URL's domain must belong to one of the instance's associated domains
    (primary or satellite); otherwise the redirect will be rejected when the
    task ticket is consumed.
    """
    session_max_duration_in_seconds: NotRequired[int]
    r"""The maximum duration that the session which will be created by the generated agent task should last.
    By default, the duration of a session created via an agent task lasts 30 minutes.
    """


class CreateAgentTaskRequestBody(BaseModel):
    on_behalf_of: OnBehalfOf
    r"""Identifies the user on whose behalf the agent task is created.
    Exactly one of user_id or identifier must be provided.
    """

    permissions: CreateAgentTaskPermissions
    r"""The permissions granted to the agent task. Must be \"*\" (all permissions)."""

    agent_name: str
    r"""A name identifying the agent. Used to derive a stable agent_id per instance.
    Logged for audit purposes.
    """

    task_description: str
    r"""A description of the task being performed. Logged for audit purposes."""

    redirect_url: str
    r"""The URL the user is redirected to after the agent task is accepted.
    Must be a valid absolute URL with an `https` scheme in production instances.
    In development instances, `http` is also permitted.
    The URL's domain must belong to one of the instance's associated domains
    (primary or satellite); otherwise the redirect will be rejected when the
    task ticket is consumed.
    """

    session_max_duration_in_seconds: Optional[int] = 1800
    r"""The maximum duration that the session which will be created by the generated agent task should last.
    By default, the duration of a session created via an agent task lasts 30 minutes.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["session_max_duration_in_seconds"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
