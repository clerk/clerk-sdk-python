"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class Action(str, Enum):
    r"""Whether to increase or decrease the credit balance."""

    INCREASE = "increase"
    DECREASE = "decrease"


class AdjustCreditBalanceRequestTypedDict(TypedDict):
    amount: int
    r"""The credit amount in cents. Must be greater than zero."""
    action: Action
    r"""Whether to increase or decrease the credit balance."""
    idempotency_key: str
    r"""A unique key to ensure the adjustment is applied only once. Repeated requests with the same key return the original ledger entry."""
    currency: NotRequired[str]
    r"""The currency code (e.g. \"USD\"). Defaults to USD if not provided."""
    note: NotRequired[str]
    r"""An optional note to attach to the ledger entry."""


class AdjustCreditBalanceRequest(BaseModel):
    amount: int
    r"""The credit amount in cents. Must be greater than zero."""

    action: Action
    r"""Whether to increase or decrease the credit balance."""

    idempotency_key: str
    r"""A unique key to ensure the adjustment is applied only once. Repeated requests with the same key return the original ledger entry."""

    currency: Optional[str] = None
    r"""The currency code (e.g. \"USD\"). Defaults to USD if not provided."""

    note: Optional[str] = None
    r"""An optional note to attach to the ledger entry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["currency", "note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
