"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .commercemoneyresponse import CommerceMoneyResponse, CommerceMoneyResponseTypedDict
from .commerceperunittotal import CommercePerUnitTotal, CommercePerUnitTotalTypedDict
from .featureresponse_2 import FeatureResponse2, FeatureResponse2TypedDict
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class CommerceSubscriptionItem2Object(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    COMMERCE_SUBSCRIPTION_ITEM = "commerce_subscription_item"


class CommerceSubscriptionItem2Status(str, Enum):
    r"""Current status of the subscription item."""

    ACTIVE = "active"
    ENDED = "ended"
    PAST_DUE = "past_due"
    UPCOMING = "upcoming"
    INCOMPLETE = "incomplete"
    ABANDONED = "abandoned"


class CommerceSubscriptionItem2CreditAmountTypedDict(TypedDict):
    r"""Credit amount."""

    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2CreditAmount(BaseModel):
    r"""Credit amount."""

    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CreditTypedDict(TypedDict):
    r"""Credit information (only available in PaymentAttempt events)."""

    amount: NotRequired[Nullable[CommerceSubscriptionItem2CreditAmountTypedDict]]
    r"""Credit amount."""
    cycle_remaining_percent: NotRequired[float]
    r"""Percentage of the billing cycle remaining."""


class Credit(BaseModel):
    r"""Credit information (only available in PaymentAttempt events)."""

    amount: OptionalNullable[CommerceSubscriptionItem2CreditAmount] = UNSET
    r"""Credit amount."""

    cycle_remaining_percent: Optional[float] = None
    r"""Percentage of the billing cycle remaining."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "cycle_remaining_percent"])
        nullable_fields = set(["amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CommerceSubscriptionItem2PlanObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    COMMERCE_PLAN = "commerce_plan"


class CommerceSubscriptionItem2FeeTypedDict(TypedDict):
    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2Fee(BaseModel):
    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2AnnualMonthlyFeeTypedDict(TypedDict):
    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2AnnualMonthlyFee(BaseModel):
    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2AnnualFeeTypedDict(TypedDict):
    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2AnnualFee(BaseModel):
    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2PlanTypedDict(TypedDict):
    r"""The associated commerce plan."""

    object: CommerceSubscriptionItem2PlanObject
    r"""String representing the object's type. Objects of the same type share the same value."""
    id: str
    r"""Unique identifier for the commerce plan."""
    name: str
    r"""The name of the commerce plan."""
    fee: Nullable[CommerceSubscriptionItem2FeeTypedDict]
    annual_monthly_fee: Nullable[CommerceSubscriptionItem2AnnualMonthlyFeeTypedDict]
    annual_fee: Nullable[CommerceSubscriptionItem2AnnualFeeTypedDict]
    amount: int
    r"""The amount in cents for the plan."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    annual_monthly_amount: int
    r"""The monthly amount in cents when billed annually."""
    annual_monthly_amount_formatted: str
    r"""The formatted annual monthly amount as a string."""
    annual_amount: int
    r"""The total annual amount in cents."""
    annual_amount_formatted: str
    r"""The formatted annual amount as a string."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    description: str
    r"""The description of the commerce plan."""
    product_id: str
    r"""The ID of the product this plan belongs to."""
    is_default: bool
    r"""Whether this is the default plan."""
    is_recurring: bool
    r"""Whether this is a recurring plan."""
    publicly_visible: bool
    r"""Whether this plan is publicly visible."""
    has_base_fee: bool
    r"""Whether this plan has a base fee."""
    payer_type: List[str]
    r"""The types of payers that can use this plan."""
    for_payer_type: str
    r"""The payer type this plan is designed for."""
    slug: str
    r"""The URL-friendly slug for the plan."""
    avatar_url: str
    r"""The URL of the plan's avatar image."""
    features: List[FeatureResponse2TypedDict]
    r"""The features included in this plan."""
    period: NotRequired[str]
    r"""The billing period for the plan."""
    interval: NotRequired[int]
    r"""The billing interval."""
    free_trial_enabled: NotRequired[bool]
    r"""Whether free trial is enabled for this plan."""
    free_trial_days: NotRequired[Nullable[int]]
    r"""Number of free trial days for this plan."""


class CommerceSubscriptionItem2Plan(BaseModel):
    r"""The associated commerce plan."""

    object: CommerceSubscriptionItem2PlanObject
    r"""String representing the object's type. Objects of the same type share the same value."""

    id: str
    r"""Unique identifier for the commerce plan."""

    name: str
    r"""The name of the commerce plan."""

    fee: Nullable[CommerceSubscriptionItem2Fee]

    annual_monthly_fee: Nullable[CommerceSubscriptionItem2AnnualMonthlyFee]

    annual_fee: Nullable[CommerceSubscriptionItem2AnnualFee]

    amount: int
    r"""The amount in cents for the plan."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    annual_monthly_amount: int
    r"""The monthly amount in cents when billed annually."""

    annual_monthly_amount_formatted: str
    r"""The formatted annual monthly amount as a string."""

    annual_amount: int
    r"""The total annual amount in cents."""

    annual_amount_formatted: str
    r"""The formatted annual amount as a string."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    description: str
    r"""The description of the commerce plan."""

    product_id: str
    r"""The ID of the product this plan belongs to."""

    is_default: bool
    r"""Whether this is the default plan."""

    is_recurring: bool
    r"""Whether this is a recurring plan."""

    publicly_visible: bool
    r"""Whether this plan is publicly visible."""

    has_base_fee: bool
    r"""Whether this plan has a base fee."""

    payer_type: List[str]
    r"""The types of payers that can use this plan."""

    for_payer_type: str
    r"""The payer type this plan is designed for."""

    slug: str
    r"""The URL-friendly slug for the plan."""

    avatar_url: str
    r"""The URL of the plan's avatar image."""

    features: List[FeatureResponse2]
    r"""The features included in this plan."""

    period: Optional[str] = None
    r"""The billing period for the plan."""

    interval: Optional[int] = None
    r"""The billing interval."""

    free_trial_enabled: Optional[bool] = None
    r"""Whether free trial is enabled for this plan."""

    free_trial_days: OptionalNullable[int] = UNSET
    r"""Number of free trial days for this plan."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["period", "interval", "free_trial_enabled", "free_trial_days"]
        )
        nullable_fields = set(
            ["fee", "annual_monthly_fee", "annual_fee", "free_trial_days"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CommerceSubscriptionItem2PlanPeriod(str, Enum):
    r"""The billing period for this subscription."""

    MONTH = "month"
    ANNUAL = "annual"


class CommerceSubscriptionItem2PaymentSourceObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    COMMERCE_SOURCE = "commerce_source"


class PaymentMethod(str, Enum):
    r"""The payment method type."""

    CARD = "card"
    APPLE_PAY = "apple_pay"
    GOOGLE_PAY = "google_pay"


class CommerceSubscriptionItem2PaymentSourceStatus(str, Enum):
    r"""Status of the payment source."""

    ACTIVE = "active"
    DISCONNECTED = "disconnected"


class PaymentSourceTypedDict(TypedDict):
    r"""The payment source associated with this subscription."""

    object: CommerceSubscriptionItem2PaymentSourceObject
    r"""String representing the object's type. Objects of the same type share the same value."""
    id: str
    r"""Unique identifier for the payment source."""
    payer_id: str
    r"""Unique identifier for the payer."""
    payment_method: PaymentMethod
    r"""The payment method type."""
    gateway: str
    r"""The payment gateway."""
    gateway_external_id: str
    r"""External ID in the payment gateway."""
    last4: str
    r"""Last 4 digits of the card (for card payment sources)."""
    status: CommerceSubscriptionItem2PaymentSourceStatus
    r"""Status of the payment source."""
    wallet_type: str
    r"""Type of wallet (if applicable)."""
    card_type: str
    r"""Type of card (if applicable)."""
    created_at: int
    r"""Unix timestamp (in milliseconds) when the payment source was created."""
    updated_at: int
    r"""Unix timestamp (in milliseconds) when the payment source was last updated."""
    is_default: NotRequired[Nullable[bool]]
    r"""Whether this is the default payment source for the payer."""
    gateway_external_account_id: NotRequired[Nullable[str]]
    r"""External account ID in the payment gateway."""
    expiry_year: NotRequired[int]
    r"""Card expiration year (for card payment sources)."""
    expiry_month: NotRequired[int]
    r"""Card expiration month (for card payment sources)."""
    is_removable: NotRequired[Nullable[bool]]
    r"""Whether this payment source can be removed."""


class PaymentSource(BaseModel):
    r"""The payment source associated with this subscription."""

    object: CommerceSubscriptionItem2PaymentSourceObject
    r"""String representing the object's type. Objects of the same type share the same value."""

    id: str
    r"""Unique identifier for the payment source."""

    payer_id: str
    r"""Unique identifier for the payer."""

    payment_method: PaymentMethod
    r"""The payment method type."""

    gateway: str
    r"""The payment gateway."""

    gateway_external_id: str
    r"""External ID in the payment gateway."""

    last4: str
    r"""Last 4 digits of the card (for card payment sources)."""

    status: CommerceSubscriptionItem2PaymentSourceStatus
    r"""Status of the payment source."""

    wallet_type: str
    r"""Type of wallet (if applicable)."""

    card_type: str
    r"""Type of card (if applicable)."""

    created_at: int
    r"""Unix timestamp (in milliseconds) when the payment source was created."""

    updated_at: int
    r"""Unix timestamp (in milliseconds) when the payment source was last updated."""

    is_default: OptionalNullable[bool] = UNSET
    r"""Whether this is the default payment source for the payer."""

    gateway_external_account_id: OptionalNullable[str] = UNSET
    r"""External account ID in the payment gateway."""

    expiry_year: Optional[int] = None
    r"""Card expiration year (for card payment sources)."""

    expiry_month: Optional[int] = None
    r"""Card expiration month (for card payment sources)."""

    is_removable: OptionalNullable[bool] = UNSET
    r"""Whether this payment source can be removed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "is_default",
                "gateway_external_account_id",
                "expiry_year",
                "expiry_month",
                "is_removable",
            ]
        )
        nullable_fields = set(
            ["is_default", "gateway_external_account_id", "is_removable"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class LifetimePaidTypedDict(TypedDict):
    r"""Total amount paid over the lifetime of this subscription."""

    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class LifetimePaid(BaseModel):
    r"""Total amount paid over the lifetime of this subscription."""

    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2AmountTypedDict(TypedDict):
    r"""Current amount for this subscription."""

    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2Amount(BaseModel):
    r"""Current amount for this subscription."""

    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2NextInvoiceAmountTypedDict(TypedDict):
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2NextInvoiceAmount(BaseModel):
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2ProrationTypedDict(TypedDict):
    amount: CommerceMoneyResponseTypedDict
    cycle_days_remaining: int
    cycle_days_total: int
    cycle_remaining_percent: float


class CommerceSubscriptionItem2Proration(BaseModel):
    amount: CommerceMoneyResponse

    cycle_days_remaining: int

    cycle_days_total: int

    cycle_remaining_percent: float


class CommerceSubscriptionItem2PayerTypedDict(TypedDict):
    remaining_balance: CommerceMoneyResponseTypedDict
    applied_amount: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2Payer(BaseModel):
    remaining_balance: CommerceMoneyResponse

    applied_amount: CommerceMoneyResponse


class CommerceSubscriptionItem2CreditsTypedDict(TypedDict):
    proration: Nullable[CommerceSubscriptionItem2ProrationTypedDict]
    payer: Nullable[CommerceSubscriptionItem2PayerTypedDict]
    total: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2Credits(BaseModel):
    proration: Nullable[CommerceSubscriptionItem2Proration]

    payer: Nullable[CommerceSubscriptionItem2Payer]

    total: CommerceMoneyResponse

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CommerceSubscriptionItem2NextInvoiceProrationTypedDict(TypedDict):
    amount: CommerceMoneyResponseTypedDict
    cycle_days_passed: int
    cycle_days_total: int
    cycle_passed_percent: float


class CommerceSubscriptionItem2NextInvoiceProration(BaseModel):
    amount: CommerceMoneyResponse

    cycle_days_passed: int

    cycle_days_total: int

    cycle_passed_percent: float


class CommerceSubscriptionItem2DiscountsTypedDict(TypedDict):
    proration: Nullable[CommerceSubscriptionItem2NextInvoiceProrationTypedDict]
    total: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2Discounts(BaseModel):
    proration: Nullable[CommerceSubscriptionItem2NextInvoiceProration]

    total: CommerceMoneyResponse

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CommerceSubscriptionItem2TotalsTypedDict(TypedDict):
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    subtotal: CommerceMoneyResponseTypedDict
    base_fee: CommerceMoneyResponseTypedDict
    tax_total: CommerceMoneyResponseTypedDict
    grand_total: CommerceMoneyResponseTypedDict
    per_unit_totals: NotRequired[List[CommercePerUnitTotalTypedDict]]
    credits: NotRequired[Nullable[CommerceSubscriptionItem2CreditsTypedDict]]
    discounts: NotRequired[Nullable[CommerceSubscriptionItem2DiscountsTypedDict]]


class CommerceSubscriptionItem2Totals(BaseModel):
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    subtotal: CommerceMoneyResponse

    base_fee: CommerceMoneyResponse

    tax_total: CommerceMoneyResponse

    grand_total: CommerceMoneyResponse

    per_unit_totals: Optional[List[CommercePerUnitTotal]] = None

    credits: OptionalNullable[CommerceSubscriptionItem2Credits] = UNSET

    discounts: OptionalNullable[CommerceSubscriptionItem2Discounts] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["per_unit_totals", "credits", "discounts"])
        nullable_fields = set(["credits", "discounts"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class NextInvoiceTypedDict(TypedDict):
    r"""Information about the next invoice."""

    amount: NotRequired[Nullable[CommerceSubscriptionItem2NextInvoiceAmountTypedDict]]
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""
    date_: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) for the next payment date."""
    per_unit_totals: NotRequired[List[CommercePerUnitTotalTypedDict]]
    r"""Per-unit total breakdown (for example, seats) for the next payment."""
    totals: NotRequired[Nullable[CommerceSubscriptionItem2TotalsTypedDict]]
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""


class NextInvoice(BaseModel):
    r"""Information about the next invoice."""

    amount: OptionalNullable[CommerceSubscriptionItem2NextInvoiceAmount] = UNSET
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    date_: Annotated[OptionalNullable[int], pydantic.Field(alias="date")] = UNSET
    r"""Unix timestamp (in milliseconds) for the next payment date."""

    per_unit_totals: Optional[List[CommercePerUnitTotal]] = None
    r"""Per-unit total breakdown (for example, seats) for the next payment."""

    totals: OptionalNullable[CommerceSubscriptionItem2Totals] = UNSET
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "date", "per_unit_totals", "totals"])
        nullable_fields = set(["amount", "date", "totals"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CommerceSubscriptionItem2NextPaymentAmountTypedDict(TypedDict):
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    amount: int
    r"""The amount in cents."""
    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""
    currency: str
    r"""The currency code (e.g., \"USD\")."""
    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2NextPaymentAmount(BaseModel):
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    amount: int
    r"""The amount in cents."""

    amount_formatted: str
    r"""The formatted amount as a string (e.g., \"$49.99\")."""

    currency: str
    r"""The currency code (e.g., \"USD\")."""

    currency_symbol: str
    r"""The currency symbol (e.g., \"$\")."""


class CommerceSubscriptionItem2NextPaymentProrationTypedDict(TypedDict):
    amount: CommerceMoneyResponseTypedDict
    cycle_days_remaining: int
    cycle_days_total: int
    cycle_remaining_percent: float


class CommerceSubscriptionItem2NextPaymentProration(BaseModel):
    amount: CommerceMoneyResponse

    cycle_days_remaining: int

    cycle_days_total: int

    cycle_remaining_percent: float


class CommerceSubscriptionItem2NextPaymentPayerTypedDict(TypedDict):
    remaining_balance: CommerceMoneyResponseTypedDict
    applied_amount: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2NextPaymentPayer(BaseModel):
    remaining_balance: CommerceMoneyResponse

    applied_amount: CommerceMoneyResponse


class CommerceSubscriptionItem2NextPaymentCreditsTypedDict(TypedDict):
    proration: Nullable[CommerceSubscriptionItem2NextPaymentProrationTypedDict]
    payer: Nullable[CommerceSubscriptionItem2NextPaymentPayerTypedDict]
    total: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2NextPaymentCredits(BaseModel):
    proration: Nullable[CommerceSubscriptionItem2NextPaymentProration]

    payer: Nullable[CommerceSubscriptionItem2NextPaymentPayer]

    total: CommerceMoneyResponse

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CommerceSubscriptionItem2NextPaymentTotalsProrationTypedDict(TypedDict):
    amount: CommerceMoneyResponseTypedDict
    cycle_days_passed: int
    cycle_days_total: int
    cycle_passed_percent: float


class CommerceSubscriptionItem2NextPaymentTotalsProration(BaseModel):
    amount: CommerceMoneyResponse

    cycle_days_passed: int

    cycle_days_total: int

    cycle_passed_percent: float


class CommerceSubscriptionItem2NextPaymentDiscountsTypedDict(TypedDict):
    proration: Nullable[CommerceSubscriptionItem2NextPaymentTotalsProrationTypedDict]
    total: CommerceMoneyResponseTypedDict


class CommerceSubscriptionItem2NextPaymentDiscounts(BaseModel):
    proration: Nullable[CommerceSubscriptionItem2NextPaymentTotalsProration]

    total: CommerceMoneyResponse

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class CommerceSubscriptionItem2NextPaymentTotalsTypedDict(TypedDict):
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    subtotal: CommerceMoneyResponseTypedDict
    base_fee: CommerceMoneyResponseTypedDict
    tax_total: CommerceMoneyResponseTypedDict
    grand_total: CommerceMoneyResponseTypedDict
    per_unit_totals: NotRequired[List[CommercePerUnitTotalTypedDict]]
    credits: NotRequired[Nullable[CommerceSubscriptionItem2NextPaymentCreditsTypedDict]]
    discounts: NotRequired[
        Nullable[CommerceSubscriptionItem2NextPaymentDiscountsTypedDict]
    ]


class CommerceSubscriptionItem2NextPaymentTotals(BaseModel):
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    subtotal: CommerceMoneyResponse

    base_fee: CommerceMoneyResponse

    tax_total: CommerceMoneyResponse

    grand_total: CommerceMoneyResponse

    per_unit_totals: Optional[List[CommercePerUnitTotal]] = None

    credits: OptionalNullable[CommerceSubscriptionItem2NextPaymentCredits] = UNSET

    discounts: OptionalNullable[CommerceSubscriptionItem2NextPaymentDiscounts] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["per_unit_totals", "credits", "discounts"])
        nullable_fields = set(["credits", "discounts"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CommerceSubscriptionItem2NextPaymentTypedDict(TypedDict):
    r"""Information about the next payment."""

    amount: NotRequired[Nullable[CommerceSubscriptionItem2NextPaymentAmountTypedDict]]
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""
    date_: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) for the next payment date."""
    per_unit_totals: NotRequired[List[CommercePerUnitTotalTypedDict]]
    r"""Per-unit total breakdown (for example, seats) for the next payment."""
    totals: NotRequired[Nullable[CommerceSubscriptionItem2NextPaymentTotalsTypedDict]]
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""


class CommerceSubscriptionItem2NextPayment(BaseModel):
    r"""Information about the next payment."""

    amount: OptionalNullable[CommerceSubscriptionItem2NextPaymentAmount] = UNSET
    r"""Base plan fee for the next payment. Does not include per-unit (e.g. seat) charges; see `totals.grand_total` for the full amount."""

    date_: Annotated[OptionalNullable[int], pydantic.Field(alias="date")] = UNSET
    r"""Unix timestamp (in milliseconds) for the next payment date."""

    per_unit_totals: Optional[List[CommercePerUnitTotal]] = None
    r"""Per-unit total breakdown (for example, seats) for the next payment."""

    totals: OptionalNullable[CommerceSubscriptionItem2NextPaymentTotals] = UNSET
    r"""Breakdown of the recurring amount that will be billed at renewal (base fee + per-unit charges). Tax and credits are not previewed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["amount", "date", "per_unit_totals", "totals"])
        nullable_fields = set(["amount", "date", "totals"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class CommerceSubscriptionItem2PayerObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    COMMERCE_PAYER = "commerce_payer"


class PayerTypedDict(TypedDict):
    r"""The payer associated with this subscription."""

    object: CommerceSubscriptionItem2PayerObject
    r"""String representing the object's type. Objects of the same type share the same value."""
    id: str
    r"""Unique identifier for the payer."""
    instance_id: str
    r"""Unique identifier for the Clerk instance."""
    first_name: str
    r"""First name of the payer."""
    last_name: str
    r"""Last name of the payer."""
    email: str
    r"""Email address of the payer."""
    image_url: str
    r"""URL of the payer's image/avatar."""
    created_at: int
    r"""Unix timestamp (in milliseconds) when the payer was created."""
    updated_at: int
    r"""Unix timestamp (in milliseconds) when the payer was last updated."""
    user_id: NotRequired[str]
    r"""User ID for user-type payers."""
    organization_id: NotRequired[str]
    r"""Organization ID for org-type payers."""
    organization_name: NotRequired[str]
    r"""Organization name for org-type payers."""
    credits_balance: NotRequired[CommerceMoneyResponseTypedDict]


class Payer(BaseModel):
    r"""The payer associated with this subscription."""

    object: CommerceSubscriptionItem2PayerObject
    r"""String representing the object's type. Objects of the same type share the same value."""

    id: str
    r"""Unique identifier for the payer."""

    instance_id: str
    r"""Unique identifier for the Clerk instance."""

    first_name: str
    r"""First name of the payer."""

    last_name: str
    r"""Last name of the payer."""

    email: str
    r"""Email address of the payer."""

    image_url: str
    r"""URL of the payer's image/avatar."""

    created_at: int
    r"""Unix timestamp (in milliseconds) when the payer was created."""

    updated_at: int
    r"""Unix timestamp (in milliseconds) when the payer was last updated."""

    user_id: Optional[str] = None
    r"""User ID for user-type payers."""

    organization_id: Optional[str] = None
    r"""Organization ID for org-type payers."""

    organization_name: Optional[str] = None
    r"""Organization name for org-type payers."""

    credits_balance: Optional[CommerceMoneyResponse] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["user_id", "organization_id", "organization_name", "credits_balance"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CommerceSubscriptionItem2TypedDict(TypedDict):
    r"""A commerce subscription item."""

    object: CommerceSubscriptionItem2Object
    r"""String representing the object's type. Objects of the same type share the same value."""
    id: str
    r"""Unique identifier for the subscription item."""
    instance_id: str
    r"""Unique identifier for the Clerk instance."""
    status: CommerceSubscriptionItem2Status
    r"""Current status of the subscription item."""
    plan_id: str
    r"""Unique identifier for the associated plan."""
    plan: Nullable[CommerceSubscriptionItem2PlanTypedDict]
    r"""The associated commerce plan."""
    plan_period: CommerceSubscriptionItem2PlanPeriod
    r"""The billing period for this subscription."""
    payment_source_id: str
    r"""Unique identifier for the payment source."""
    payer_id: str
    r"""Unique identifier for the payer."""
    is_free_trial: bool
    r"""Whether this subscription is currently on a free trial."""
    proration_date: str
    r"""Date used for proration calculations."""
    created_at: int
    r"""Unix timestamp (in milliseconds) when the subscription was created."""
    updated_at: int
    r"""Unix timestamp (in milliseconds) when the subscription was last updated."""
    credit: NotRequired[Nullable[CreditTypedDict]]
    r"""Credit information (only available in PaymentAttempt events)."""
    price_id: NotRequired[str]
    r"""Unique identifier for the associated price"""
    payment_source: NotRequired[Nullable[PaymentSourceTypedDict]]
    r"""The payment source associated with this subscription."""
    lifetime_paid: NotRequired[Nullable[LifetimePaidTypedDict]]
    r"""Total amount paid over the lifetime of this subscription."""
    amount: NotRequired[Nullable[CommerceSubscriptionItem2AmountTypedDict]]
    r"""Current amount for this subscription."""
    next_invoice: NotRequired[Nullable[NextInvoiceTypedDict]]
    r"""Information about the next invoice."""
    next_payment: NotRequired[Nullable[CommerceSubscriptionItem2NextPaymentTypedDict]]
    r"""Information about the next payment."""
    payer: NotRequired[Nullable[PayerTypedDict]]
    r"""The payer associated with this subscription."""
    period_start: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) when the current period started."""
    period_end: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) when the current period ends."""
    canceled_at: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) when the subscription was canceled."""
    past_due_at: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) when the subscription became past due."""
    ended_at: NotRequired[Nullable[int]]
    r"""Unix timestamp (in milliseconds) when the subscription ended."""


class CommerceSubscriptionItem2(BaseModel):
    r"""A commerce subscription item."""

    object: CommerceSubscriptionItem2Object
    r"""String representing the object's type. Objects of the same type share the same value."""

    id: str
    r"""Unique identifier for the subscription item."""

    instance_id: str
    r"""Unique identifier for the Clerk instance."""

    status: CommerceSubscriptionItem2Status
    r"""Current status of the subscription item."""

    plan_id: str
    r"""Unique identifier for the associated plan."""

    plan: Nullable[CommerceSubscriptionItem2Plan]
    r"""The associated commerce plan."""

    plan_period: CommerceSubscriptionItem2PlanPeriod
    r"""The billing period for this subscription."""

    payment_source_id: str
    r"""Unique identifier for the payment source."""

    payer_id: str
    r"""Unique identifier for the payer."""

    is_free_trial: bool
    r"""Whether this subscription is currently on a free trial."""

    proration_date: str
    r"""Date used for proration calculations."""

    created_at: int
    r"""Unix timestamp (in milliseconds) when the subscription was created."""

    updated_at: int
    r"""Unix timestamp (in milliseconds) when the subscription was last updated."""

    credit: OptionalNullable[Credit] = UNSET
    r"""Credit information (only available in PaymentAttempt events)."""

    price_id: Optional[str] = None
    r"""Unique identifier for the associated price"""

    payment_source: OptionalNullable[PaymentSource] = UNSET
    r"""The payment source associated with this subscription."""

    lifetime_paid: OptionalNullable[LifetimePaid] = UNSET
    r"""Total amount paid over the lifetime of this subscription."""

    amount: OptionalNullable[CommerceSubscriptionItem2Amount] = UNSET
    r"""Current amount for this subscription."""

    next_invoice: OptionalNullable[NextInvoice] = UNSET
    r"""Information about the next invoice."""

    next_payment: OptionalNullable[CommerceSubscriptionItem2NextPayment] = UNSET
    r"""Information about the next payment."""

    payer: OptionalNullable[Payer] = UNSET
    r"""The payer associated with this subscription."""

    period_start: OptionalNullable[int] = UNSET
    r"""Unix timestamp (in milliseconds) when the current period started."""

    period_end: OptionalNullable[int] = UNSET
    r"""Unix timestamp (in milliseconds) when the current period ends."""

    canceled_at: OptionalNullable[int] = UNSET
    r"""Unix timestamp (in milliseconds) when the subscription was canceled."""

    past_due_at: OptionalNullable[int] = UNSET
    r"""Unix timestamp (in milliseconds) when the subscription became past due."""

    ended_at: OptionalNullable[int] = UNSET
    r"""Unix timestamp (in milliseconds) when the subscription ended."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "credit",
                "price_id",
                "payment_source",
                "lifetime_paid",
                "amount",
                "next_invoice",
                "next_payment",
                "payer",
                "period_start",
                "period_end",
                "canceled_at",
                "past_due_at",
                "ended_at",
            ]
        )
        nullable_fields = set(
            [
                "credit",
                "plan",
                "payment_source",
                "lifetime_paid",
                "amount",
                "next_invoice",
                "next_payment",
                "payer",
                "period_start",
                "period_end",
                "canceled_at",
                "past_due_at",
                "ended_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    NextInvoice.model_rebuild()
except NameError:
    pass
try:
    CommerceSubscriptionItem2NextPayment.model_rebuild()
except NameError:
    pass
