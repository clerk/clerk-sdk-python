"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from enum import Enum
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class CreateRoleSetType(str, Enum):
    r"""The type of the role set. \"initial\" role sets are the default for new organizations.
    Only one role set can be \"initial\" per instance.
    """

    INITIAL = "initial"
    CUSTOM = "custom"


class CreateRoleSetRequestBodyTypedDict(TypedDict):
    name: str
    r"""The name of the new role set"""
    default_role_key: str
    r"""The key of the role to use as the default role for new organization members.
    Must be one of the roles in the `roles` array.
    """
    creator_role_key: str
    r"""The key of the role to assign to organization creators.
    Must be one of the roles in the `roles` array.
    """
    roles: List[str]
    r"""Array of role keys to include in the role set.
    Must contain at least one role and no more than 10 roles.
    """
    key: NotRequired[str]
    r"""A unique key for the role set. Must start with 'role_set:' and contain only lowercase alphanumeric characters and underscores.
    If not provided, a key will be generated from the name.
    """
    description: NotRequired[Nullable[str]]
    r"""Optional description for the role set"""
    type: NotRequired[CreateRoleSetType]
    r"""The type of the role set. \"initial\" role sets are the default for new organizations.
    Only one role set can be \"initial\" per instance.
    """


class CreateRoleSetRequestBody(BaseModel):
    name: str
    r"""The name of the new role set"""

    default_role_key: str
    r"""The key of the role to use as the default role for new organization members.
    Must be one of the roles in the `roles` array.
    """

    creator_role_key: str
    r"""The key of the role to assign to organization creators.
    Must be one of the roles in the `roles` array.
    """

    roles: List[str]
    r"""Array of role keys to include in the role set.
    Must contain at least one role and no more than 10 roles.
    """

    key: Optional[str] = None
    r"""A unique key for the role set. Must start with 'role_set:' and contain only lowercase alphanumeric characters and underscores.
    If not provided, a key will be generated from the name.
    """

    description: OptionalNullable[str] = UNSET
    r"""Optional description for the role set"""

    type: Optional[CreateRoleSetType] = None
    r"""The type of the role set. \"initial\" role sets are the default for new organizations.
    Only one role set can be \"initial\" per instance.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["key", "description", "type"])
        nullable_fields = set(["description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
