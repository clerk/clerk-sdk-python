"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class RecordType(str, Enum):
    CNAME = "CNAME"
    TXT = "TXT"


class AutomationDisposition(str, Enum):
    ALREADY_SATISFIED = "already_satisfied"
    SAFE_TO_CREATE = "safe_to_create"
    MANUAL_RESOLUTION_REQUIRED = "manual_resolution_required"


class DNSTargetTypedDict(TypedDict):
    host: str
    value: str
    record_type: RecordType
    required: bool
    r"""Denotes whether this DNS target is required to be set in order for the domain to be considered deployed.

    """
    automation_disposition: NotRequired[AutomationDisposition]


class DNSTarget(BaseModel):
    host: str

    value: str

    record_type: RecordType

    required: bool
    r"""Denotes whether this DNS target is required to be set in order for the domain to be considered deployed.

    """

    automation_disposition: Optional[AutomationDisposition] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["automation_disposition"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
