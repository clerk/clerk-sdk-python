"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, Nullable, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing_extensions import TypedDict


class ReverificationObject(str, Enum):
    r"""String representing the object's type. Objects of the same type share the same value."""

    REVERIFICATION = "reverification"


class Level(str, Enum):
    r"""The level used for the reverification"""

    FIRST_FACTOR = "first_factor"
    SECOND_FACTOR = "second_factor"
    MULTI_FACTOR = "multi_factor"


class ReverificationStatus(str, Enum):
    NEEDS_FIRST_FACTOR = "needs_first_factor"
    NEEDS_SECOND_FACTOR = "needs_second_factor"
    COMPLETE = "complete"


class ReverificationTypedDict(TypedDict):
    r"""A reverification scoped to a session. Returned so a resource server can validate a reverification id received from its client."""

    object: ReverificationObject
    r"""String representing the object's type. Objects of the same type share the same value.

    """
    id: str
    session_id: str
    user_id: str
    level: Level
    r"""The level used for the reverification"""
    status: ReverificationStatus
    first_factor_verified_at: Nullable[int]
    r"""Unix timestamp (ms) of the first factor verification, or null if not verified.

    """
    second_factor_verified_at: Nullable[int]
    r"""Unix timestamp (ms) of the second factor verification, or null if not verified.

    """
    created_at: int
    r"""Unix timestamp of creation.

    """
    updated_at: int
    r"""Unix timestamp of last update.

    """


class Reverification(BaseModel):
    r"""A reverification scoped to a session. Returned so a resource server can validate a reverification id received from its client."""

    object: ReverificationObject
    r"""String representing the object's type. Objects of the same type share the same value.

    """

    id: str

    session_id: str

    user_id: str

    level: Level
    r"""The level used for the reverification"""

    status: ReverificationStatus

    first_factor_verified_at: Nullable[int]
    r"""Unix timestamp (ms) of the first factor verification, or null if not verified.

    """

    second_factor_verified_at: Nullable[int]
    r"""Unix timestamp (ms) of the second factor verification, or null if not verified.

    """

    created_at: int
    r"""Unix timestamp of creation.

    """

    updated_at: int
    r"""Unix timestamp of last update.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m
