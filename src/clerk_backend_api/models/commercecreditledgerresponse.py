"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from datetime import datetime
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class CommerceCreditLedgerResponseTypedDict(TypedDict):
    r"""A credit ledger entry."""

    object: str
    r"""String representing the object's type. Always \"commerce_credit_ledger\"."""
    id: str
    r"""Unique identifier for the ledger entry."""
    payer_id: str
    r"""The ID of the payer whose balance was adjusted."""
    amount: int
    r"""The signed credit amount. Positive for increases, negative for decreases."""
    currency: str
    r"""The currency code of the credit adjustment."""
    source_type: str
    r"""The type of source that originated the adjustment (e.g. \"grant\")."""
    source_id: str
    r"""The ID of the source that originated the adjustment."""
    created_at: datetime
    r"""Timestamp when the ledger entry was created."""
    note: NotRequired[Nullable[str]]
    r"""An optional note attached to the ledger entry."""


class CommerceCreditLedgerResponse(BaseModel):
    r"""A credit ledger entry."""

    object: str
    r"""String representing the object's type. Always \"commerce_credit_ledger\"."""

    id: str
    r"""Unique identifier for the ledger entry."""

    payer_id: str
    r"""The ID of the payer whose balance was adjusted."""

    amount: int
    r"""The signed credit amount. Positive for increases, negative for decreases."""

    currency: str
    r"""The currency code of the credit adjustment."""

    source_type: str
    r"""The type of source that originated the adjustment (e.g. \"grant\")."""

    source_id: str
    r"""The ID of the source that originated the adjustment."""

    created_at: datetime
    r"""Timestamp when the ledger entry was created."""

    note: OptionalNullable[str] = UNSET
    r"""An optional note attached to the ledger entry."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["note"])
        nullable_fields = set(["note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
