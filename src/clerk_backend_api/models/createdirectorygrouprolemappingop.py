"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from clerk_backend_api.utils import FieldMetadata, PathParamMetadata, RequestMetadata
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class CreateDirectoryGroupRoleMappingRequestBodyTypedDict(TypedDict):
    role_id: str
    r"""The ID of the organization role to assign to members of the group."""
    directory_group_id: NotRequired[str]
    r"""The group ID from the identity provider. Exactly one of `directory_group_id` or `scim_group_id` is required."""
    scim_group_id: NotRequired[str]
    r"""The legacy name for `directory_group_id`. Send either one, or both with the same value; sending both with different values is rejected."""
    precedence: NotRequired[int]
    r"""The precedence for this mapping. Lower values take priority when a user belongs
    to multiple mapped groups. If omitted, the mapping is appended with the
    next-highest precedence.
    """


class CreateDirectoryGroupRoleMappingRequestBody(BaseModel):
    role_id: str
    r"""The ID of the organization role to assign to members of the group."""

    directory_group_id: Optional[str] = None
    r"""The group ID from the identity provider. Exactly one of `directory_group_id` or `scim_group_id` is required."""

    scim_group_id: Optional[str] = None
    r"""The legacy name for `directory_group_id`. Send either one, or both with the same value; sending both with different values is rejected."""

    precedence: Optional[int] = None
    r"""The precedence for this mapping. Lower values take priority when a user belongs
    to multiple mapped groups. If omitted, the mapping is appended with the
    next-highest precedence.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["directory_group_id", "scim_group_id", "precedence"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateDirectoryGroupRoleMappingRequestTypedDict(TypedDict):
    directory_id: str
    r"""The ID of the directory."""
    request_body: CreateDirectoryGroupRoleMappingRequestBodyTypedDict


class CreateDirectoryGroupRoleMappingRequest(BaseModel):
    directory_id: Annotated[
        str, FieldMetadata(path=PathParamMetadata(style="simple", explode=False))
    ]
    r"""The ID of the directory."""

    request_body: Annotated[
        CreateDirectoryGroupRoleMappingRequestBody,
        FieldMetadata(request=RequestMetadata(media_type="application/json")),
    ]
