"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .role import Role, RoleTypedDict
from clerk_backend_api.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class SCIMGroupRoleMappingObject(str, Enum):
    r"""String representing the object's type. Always \"scim_group_role_mapping\"."""

    SCIM_GROUP_ROLE_MAPPING = "scim_group_role_mapping"


class SCIMGroupRoleMappingTypedDict(TypedDict):
    r"""A mapping from a SCIM group to an organization role. Members of the group are assigned the role; the lowest-precedence match wins when a user belongs to multiple mapped groups."""

    object: SCIMGroupRoleMappingObject
    r"""String representing the object's type. Always \"scim_group_role_mapping\"."""
    id: str
    r"""Unique identifier for the SCIM group role mapping."""
    scim_directory_id: str
    r"""The ID of the SCIM directory this mapping belongs to."""
    scim_group_id: str
    r"""The SCIM group ID from the identity provider."""
    scim_group_display_name: str
    r"""The display name of the SCIM group, as reported by the identity provider."""
    precedence: int
    r"""Mapping precedence, starting at 1. Lower values take priority when a user belongs to multiple mapped groups."""
    created_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was created."""
    updated_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was last updated."""
    role: NotRequired[RoleTypedDict]


class SCIMGroupRoleMapping(BaseModel):
    r"""A mapping from a SCIM group to an organization role. Members of the group are assigned the role; the lowest-precedence match wins when a user belongs to multiple mapped groups."""

    object: SCIMGroupRoleMappingObject
    r"""String representing the object's type. Always \"scim_group_role_mapping\"."""

    id: str
    r"""Unique identifier for the SCIM group role mapping."""

    scim_directory_id: str
    r"""The ID of the SCIM directory this mapping belongs to."""

    scim_group_id: str
    r"""The SCIM group ID from the identity provider."""

    scim_group_display_name: str
    r"""The display name of the SCIM group, as reported by the identity provider."""

    precedence: int
    r"""Mapping precedence, starting at 1. Lower values take priority when a user belongs to multiple mapped groups."""

    created_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was created."""

    updated_at: int
    r"""Unix timestamp (milliseconds) of when the mapping was last updated."""

    role: Optional[Role] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["role"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
